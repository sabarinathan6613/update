// PLCSimulator.jsx is deprecated.
// PLC tag simulation drawer is cut because the web application's scope is strictly Cloud Database read-only access.
// Synchronization status monitoring continues to run in the background sync service context.
export default function PLCSimulator() {
  return null;
}
